{
  "name": "react-native-minuit",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "postinstall": "yarn workspaces run prepare"
  },
  "devDependencies": {
    "create-index": "^2.6.0"
  }
}
